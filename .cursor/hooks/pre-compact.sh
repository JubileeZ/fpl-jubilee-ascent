#!/usr/bin/env bash
# Cursor adapter: preCompact observability only
set -euo pipefail
cat >/dev/null
printf '{"user_message":"Context compaction starting. Ensure task.md Work Packet is current."}\n'
exit 0
